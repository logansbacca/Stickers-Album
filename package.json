{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "lint": "eslint --fix --ext .js,.html,.css .",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "prettier": "prettier --write 'src/**/*.js'",
    "check-all": "npm run prettier && npm run lint",
  },
  "devDependencies": {
    "eslint": "^8.34.0",
    "prettier": "^2.8.4",
    "prettier-eslint": "^15.0.1",
    "vite": "^4.1.0"
  }
}
